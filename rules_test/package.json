{
  "name": "kurdistan-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security-rules regression suite for firestore.rules. Runs entirely against the Firebase Emulator Suite on a demo- project id, so it never touches the live rewar-app-1c10e project.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,auth --project demo-kurdistan --config ../firebase.json \"node --test\"",
    "emulator": "firebase emulators:start --only firestore,auth --project demo-kurdistan --config ../firebase.json"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0",
    "firebase-admin": "^13.0.0"
  }
}
