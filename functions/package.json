{
  "name": "kurdistan-paradise-functions",
  "description": "Cloud Functions for the Kurdistan Paradise Travel Guide app",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "sync-airports": "node ../tool/sync_airports.js",
    "test": "node --test test/*.test.js",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  }
}
