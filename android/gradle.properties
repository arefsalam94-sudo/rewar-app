org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin 2.3.20 + AGP 9.x + Gradle 9.1 on Windows fail in
# BuildToolsApiCompilationWork: "Could not close incremental caches ...
# class-fq-name-to-source.tab". Every Kotlin plugin module dies before Dart
# code is ever compiled. Non-incremental compilation sidesteps the broken
# cache lifecycle; it only costs build time, nothing at runtime.
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process
